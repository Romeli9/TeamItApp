import {StyleSheet} from 'react-native';

import {Colors} from 'shared/libs/helpers';

export const MemberAvatarStyles = StyleSheet.create({
  required_image_2: {
    width: 50,
    height: 50,
    borderRadius: 25,
  },

  author_image: {
    width: 27,
    height: 27,
    borderRadius: 40,
  },
});
